# Test plan — CI workflow reliability with local Supabase

Created: 2026-09-25 · Last updated: 2026-09-25 · Plan path: `docs/testing/test-plan-ci-reliability.md` · Sandbox: GitHub-hosted ephemeral Ubuntu runner plus local Supabase stack; no shared tenant or production credentials · Findings precision: 4 / 4 baseline failure classes observed
Baseline: `8458b23e0418986b1d71eaf9b897e78034d3cc4a` · untracked files: 2 (planning artifacts) · fingerprint: `24a96e219bb3c9e1a3b2fbeaf51fc633054aeec17a7f6da194018ded43396918` (captured before source/config edits; planning artifacts included)

## Inventory

| Surface                 | Entry points                                                                                              | Owner module          | Notes                                                                                                                  |
| ----------------------- | --------------------------------------------------------------------------------------------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| CI required checks      | `.github/workflows/ci.yml:8`                                                                              | GitHub Actions / Jest | Runs type-check/lint/build; Jest `dom/node` use mocked boundary env while `db` runs separately against local Supabase. |
| Playwright lanes        | `.github/workflows/ci.yml:81`, `:116`; `playwright.config.ts:13`                                          | E2E/auth harness      | Separate no-auth and auth-required lanes; auth setup logs in then bootstraps canonical fixtures.                       |
| Auth fixture            | `tests/auth.setup.ts:8`, `:15`; `tests/support/auth/ensure-canonical-auth-user.ts:4`                      | Test support          | Existing helper creates or repairs a synthetic canonical user via service-role API.                                    |
| DB/RLS suite            | `jest.setup.db.js:1`; `tests/db/support/env.ts:1`; `tests/db/rate-history-scrape-attempts-rls.test.ts:32` | Jest db project       | Requires real local Supabase; local defaults are `127.0.0.1:54421` and demo keys.                                      |
| Local database baseline | `supabase/config.toml:59`, `:88`                                                                          | Supabase CLI          | Migration replay is intentionally disabled; `schemas/baseline.sql` is applied as the local seed.                       |
| PR performance jobs     | `.github/workflows/perf-pr.yml:8`, `:49`                                                                  | k6 / Lighthouse CI    | Both currently inherit placeholder Supabase env; k6 secrets are empty in this run.                                     |
| k6 smoke                | `tests/performance/k6/scenarios/smoke.js:19`, `:41`; `tests/performance/k6/lib/config.js:8`               | Performance harness   | 5 VUs for 30s; checks public endpoints and authenticated endpoints when local auth succeeds.                           |
| Lighthouse targets      | `tests/performance/lighthouse/lighthouserc.js:12`                                                         | Lighthouse CI         | `/dashboard` does not exist; dashboard content is selected at `/`, while `/transactions` and `/accounts` require auth. |
| Critical journeys       | `/auth/login` → `/api/testing/bootstrap` → `/transactions`; public `/` and rates endpoints                | User-facing web app   | Auth-required lane must remain authenticated; no bypass in this journey.                                               |

## Ranked targets

Rows are never removed by budget; budget changes order and status only. Rows contributed by a
sibling in the layer sweep name that sibling in "Sibling skill".

| Target                                                   | Blast radius                                                                 | Churn / past fixes                                                                    | Consequence class                                 | Existing evidence                                              | Altitude          | Target rung | Sibling skill                                       | Verdict | Status  | Run       |
| -------------------------------------------------------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------- | -------------------------------------------------------------- | ----------------- | ----------- | --------------------------------------------------- | ------- | ------- | --------- |
| Required Jest + RLS suite against local Supabase         | `test:db` project and required CI gate; unit `dom/node` projects stay mocked | Repeated baseline failures on main; placeholder env overrides the real local defaults | Data access/RLS correctness and CI validity       | E-CI-RLS, E-CI-MAIN; RLS suite exists                          | Integration       | L4          | `database-persistence-testing`                      | probe   | pending | ci-local  |
| Auth-required login and canonical fixture bootstrap      | All tagged authenticated E2E specs and storage state                         | Repeated baseline auth bootstrap failures                                             | Authentication boundary / test false negative     | E-CI-AUTH, existing `tests/e2e/00-supabase-auth-smoke.spec.ts` | End-to-end        | L5          | `real-run-validation`, `appsec-adversarial-auditor` | probe   | pending | ci-auth   |
| No-auth E2E against local dependencies                   | Public routes and API calls in no-auth lane                                  | Baseline no-auth lane already fails on main                                           | Visible availability / CI validity                | E-CI-MAIN                                                      | End-to-end        | L4          | `real-run-validation`                               | pin     | pending | ci-noauth |
| k6 PR smoke against local stack                          | Rates, health, and optional auth endpoints                                   | Baseline nightly/perf checks repeatedly fail; placeholder DNS is observed             | Service availability and latency                  | E-CI-K6, `smoke.js` has status and latency thresholds          | Runtime smoke     | L4          | `runtime-reliability-testing`                       | probe   | pending | ci-k6     |
| Lighthouse collection uses existing public pages         | Four Lighthouse navigation targets                                           | `/dashboard` currently produces HTTP 404                                              | Visible route availability / performance evidence | E-CI-LH                                                        | Real browser run  | L3          | `real-run-validation`                               | pin     | pending | ci-lh     |
| Local-only environment export and test-user provisioning | All local stack consumers; prevents remote/production writes                 | Existing job config silently supplies `example.supabase.co` and fake keys             | Secret/tenant boundary                            | E-CI-ENV                                                       | Boundary contract | L3          | `appsec-adversarial-auditor`                        | probe   | pending | ci-local  |
| Supabase startup and teardown                            | Required, E2E, and perf jobs                                                 | Existing eval-gate already starts/stops a local stack                                 | CI resource isolation and no leaked test state    | E-CI-LOCAL                                                     | Job lifecycle     | L2          | `docker-test-containers`                            | pin     | pending | ci-local  |

Verdicts: probe · pin · none. Statuses: pending · in progress · done · blocked · n/a.
A `none` verdict is created with status `n/a`; the execution ratio excludes `n/a` rows.

## Real-run recipes

How each critical journey is driven for real (`real-run-validation`), so EXECUTE never rediscovers it.

| Journey                       | Start command                                                                                                     | Data setup                                                                                                                                  | Sample requests                                                                                            | Expected observable                                                                            |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| Required Jest DB/RLS checks   | `npx supabase start`; local env export; `npm run test:db`; run `dom/node` projects separately                     | `supabase/config.toml` local baseline seed; unit projects use `jest.setup.js` mocks; synthetic rows are created/cleaned by DB suite         | `tests/db/rate-history-scrape-attempts-rls.test.ts` real anon/service-role requests                        | RLS assertions pass against local DB; unit mocks remain isolated; no external DNS; job exits 0 |
| Auth-required browser journey | `npx supabase start`; export local env; provision canonical user; `npm run e2e:ci:auth-required`                  | Synthetic `test@fintec.com` user via existing helper against loopback Supabase; fixture bootstrap creates only canonical account/categories | Real browser login, `POST /api/testing/bootstrap`, navigate `/transactions`                                | Login exits `/auth/`, bootstrap returns success, storage state created, tagged tests run       |
| No-auth browser journey       | `npx supabase start`; export local env; `npm run e2e:ci:no-auth`                                                  | Local stack only; `FRONTEND_AUTH_BYPASS=1` is confined to the no-auth lane                                                                  | Critical public/no-auth Playwright routes and API calls                                                    | Tests reach intended no-auth routes; no placeholder network calls                              |
| PR k6 smoke                   | `npx supabase start`; export local env; build/start app; provision canonical test user; run the existing k6 smoke | Local baseline schema plus synthetic canonical auth user; 5 VUs, 30s, existing thresholds unchanged                                         | Existing `/api/binance-rates`, `/api/bcv-rates`, `/api/scrapers/health`, and authenticated smoke endpoints | HTTP error rate and p99 meet existing thresholds; no external DNS failures                     |
| Lighthouse public pages       | `npx supabase start`; export local env; build; LHCI starts the app                                                | No user data or auth cookie                                                                                                                 | Existing public root/privacy/terms/waitlist pages                                                          | Every configured URL returns a document; no `ERRORED_DOCUMENT_REQUEST`; thresholds unchanged   |

## Layer matrix

The `Run` column identifies which bounded run owns each layer and target row; leave it blank for unscoped work.

| Layer                      | Skill                          | Scope                                                                                                     | Status | Run      |
| -------------------------- | ------------------------------ | --------------------------------------------------------------------------------------------------------- | ------ | -------- |
| Security                   | `appsec-adversarial-auditor`   | PLAN invoked inline from skill; local-only URL guard, synthetic credentials, no auth bypass, secret scan  | done   | ci-local |
| Runtime and faults         | `runtime-reliability-testing`  | PLAN invoked inline from skill; k6 smoke, HTTP error rate, p99, Lighthouse navigation                     | done   | ci-k6    |
| Persistence and migrations | `database-persistence-testing` | PLAN invoked inline from skill; local baseline schema and RLS contract suite; no migration edits          | done   | ci-local |
| Architecture conformance   | `clean-architecture-audit`     | No application architecture changes; workflow/test harness only                                           | n/a    |          |
| Critical e2e journeys      | `real-run-validation`          | PLAN invoked inline from skill; auth login/bootstrap and no-auth public route flow                        | done   | ci-auth  |
| Sandbox                    | `docker-test-containers`       | PLAN invoked inline from skill; local Supabase on ephemeral GitHub runner; always stop with `--no-backup` | done   | ci-local |

Statuses: pending · in progress · done · blocked · n/a. `plan gaps` counts a row as swept when its
status cell reads `done`, `fixed` or `closed`, and drops `n/a`, `na`, `none` and `skipped` from the
denominator entirely; the `Skill` cell only labels the rows still owed. In this scoped CI run,
architecture is `n/a` because the application layers are unchanged.

## Not testing, on purpose

| Target                                           | Reason                                                                                                     |
| ------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| Production or shared Supabase project            | No shared tenant, production credentials, or real user data are permitted in CI repair.                    |
| Remote cloud APIs and live Binance/BCV providers | The CI contract is local and deterministic; no external service availability claims.                       |
| K6 soak/spike/load thresholds                    | This PR fixes the PR smoke environment only; existing thresholds and nightly job scope remain unchanged.   |
| Lighthouse protected routes                      | No authenticated Lighthouse profile is provisioned; use existing public routes rather than bypassing auth. |
| App auth/authorization implementation            | No application auth source change is intended; only test-environment provisioning is touched.              |

## Characterization (legacy)

| Test | Behavior pinned                                                | Believed correct? | Promote or delete after the change |
| ---- | -------------------------------------------------------------- | ----------------- | ---------------------------------- |
| None | No characterization test is introduced by this CI-only change. | n/a               | n/a                                |

## Execution log

| Date       | Target                                          | Rung reached                | Findings (path:line)                                                                        | Promoted tests                                           | Evidence (ledger id)                                     | Notes                                                                                                                                                                                     |
| ---------- | ----------------------------------------------- | --------------------------- | ------------------------------------------------------------------------------------------- | -------------------------------------------------------- | -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-25 | Baseline GitHub CI on `main` (`8458b23e`)       | L1 observed CI logs         | `.github/workflows/ci.yml:13`, `:86`, `:121`; `perf-pr.yml:18`, `:58`; `lighthouserc.js:15` | Existing DB/RLS, E2E, k6, LHCI journeys                  | E-CI-AUTH, E-CI-RLS, E-CI-K6, E-CI-LH, E-CI-MAIN         | Auth/DB use placeholder host; k6 DNS fails; `/dashboard` returns 404; same CI job classes fail on baseline main.                                                                          |
| 2026-09-25 | PLAN contribution: AppSec boundary              | Plan / L3                   | `.github/workflows/ci.yml:13`, `perf-pr.yml:18`                                             | Local-only URL/service-role guard, no remote credentials | E-CI-ENV                                                 | Inline skill invocation: `/home/alesierraalta/documents/projects/fintec/.agents/skills/appsec-adversarial-auditor/SKILL.md`; contributed local-only test-host and secret-boundary target. |
| 2026-09-25 | PLAN contribution: persistence/RLS              | Plan / L4                   | `tests/db/rate-history-scrape-attempts-rls.test.ts:32`                                      | Existing RLS integration suite                           | E-CI-RLS                                                 | Inline skill invocation: `/home/alesierraalta/documents/projects/fintec/.agents/skills/database-persistence-testing/SKILL.md`; contributed real local Supabase RLS target.                |
| 2026-09-25 | PLAN contribution: runtime/performance          | Plan / L4                   | `tests/performance/k6/scenarios/smoke.js:19`                                                | Existing smoke thresholds                                | E-CI-K6                                                  | Inline skill invocation: `/home/alesierraalta/documents/projects/fintec/.agents/skills/runtime-reliability-testing/SKILL.md`; keep existing p99/error thresholds, fix environment only.   |
| 2026-09-25 | PLAN contribution: E2E real run                 | Plan / L5                   | `tests/auth.setup.ts:15`, `tests/e2e/00-supabase-auth-smoke.spec.ts:7`                      | Existing authenticated browser journey                   | E-CI-AUTH                                                | Inline skill invocation: `/home/alesierraalta/documents/projects/fintec/.agents/skills/real-run-validation/SKILL.md`; contribute real local login/bootstrap journey.                      |
| 2026-09-25 | PLAN contribution: sandbox lifecycle            | Plan / L2                   | `.github/workflows/ci.yml:65`, `:77`                                                        | Existing local stack start/stop pattern                  | E-CI-LOCAL                                               | Inline skill invocation: `/home/alesierraalta/documents/projects/fintec/.agents/skills/docker-test-containers/SKILL.md`; require test-owned local stack and cleanup.                      |
| 2026-09-25 | Local dom/node Jest selection and static checks | L4 selected unit/node tests | `.github/workflows/ci.yml:25`, `:35`; helper source and test; workflow files                | Existing suite plus helper mapping tests                 | E-CI-LOCAL-DOMNODE, E-CI-LOCAL-HELPER, E-CI-LOCAL-STATIC | Observed: 293 suites/2,092 tests passed; helper 5/5; oxlint, type-check, Prettier and diff-check exit 0. DB/Auth/k6/LH still not run locally.                                             |
| 2026-09-25 | Local env helper CLI smoke                      | L3 helper/runtime boundary  | `scripts/testing/prepare-local-supabase-ci.ts:106`                                          | Helper unit suite plus actual CLI entrypoint             | E-CI-LOCAL-ENV                                           | Observed: command exits 0 with GITHUB_ENV=/dev/null and prints no values; no stack started.                                                                                               |

## Findings

A rejected or wontfix finding is a known non-issue: it is never re-proposed unless the fingerprint
of its cited files changed; when a run skips it, it cites the row. Severity is the consequence class
(`references/prioritization.md`); always state whether data is safe. A `confirmed` or `fixed`
finding names the promoted test that asserts the promised behaviour, so it is red on the current
code and green once fixed (rule 13); a test written the other way round is a characterization
test and says so in its name. A finding that never got a test stays `open`, reason `not pinned`.

| Id    | Finding (path:line, one line)                                                                                      | Severity (consequence class)                                 | Data safe?                                      | Evidence id          | Pinning test (suite path :: test name)                                                                                        | Status    | Verdict by / date      | Reason                                                                                                                                   | Cited-files fingerprint at verdict                                 |
| ----- | ------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------ | ----------------------------------------------- | -------------------- | ----------------------------------------------------------------------------------------------------------------------------- | --------- | ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| CI-F1 | `.github/workflows/ci.yml:13` sets an unreachable placeholder host for DB/RLS Jest tests                           | Visible CI failure / persistence test unavailable            | Yes; DNS fails before any DB request            | E-CI-RLS, E-CI-MAIN  | `tests/db/rate-history-scrape-attempts-rls.test.ts :: rate-history and scrape-attempts RLS enforcement`                       | confirmed | observado / 2026-09-25 | Main and PR both fail resolving `example.supabase.co`; run the same suite against local Supabase.                                        | `2fc0ea8053977c89d0d2178b5f4d2d89a600b920a7b3e9e7555b8d8cbf01be6c` |
| CI-F2 | `.github/workflows/ci.yml:121` auth-required lane has no reachable auth service/canonical user                     | Auth journey blocked / no data mutation reaches app          | Yes; login fails before fixture bootstrap       | E-CI-AUTH, E-CI-MAIN | `tests/e2e/00-supabase-auth-smoke.spec.ts :: logs in through the real auth lane, bootstraps fixtures, and opens transactions` | confirmed | observado / 2026-09-25 | E2E setup reports `Login failed before bootstrap: Failed to fetch`; use local Supabase and create only the synthetic canonical user.     | `80b354dc27062736e0f45abb24d4fbe779dd6873e6fd354bb5d87256d15e9c92` |
| CI-F3 | `.github/workflows/perf-pr.yml:18` and `:58` keep the placeholder in build/server env while PERF secrets are unset | Runtime smoke cannot observe intended local service behavior | Yes; requests fail DNS or return service errors | E-CI-K6              | `tests/performance/k6/scenarios/smoke.js :: smoke:binance-rates: status 200`                                                  | confirmed | observado / 2026-09-25 | k6 reports `example.supabase.co` DNS failure, 67.10% request failures, p99 3.12s; configure local Supabase without weakening thresholds. | `a0e48b55c4666a208ccefd0c5609163ff8fc5a3cc152299cff18e930908e320e` |
| CI-F4 | `tests/performance/lighthouse/lighthouserc.js:15` audits nonexistent `/dashboard`                                  | Lighthouse collection fails before meaningful measurement    | Yes; only an HTTP 404                           | E-CI-LH              | `tests/performance/lighthouse/lighthouserc.js :: Lighthouse CI navigation`                                                    | confirmed | observado / 2026-09-25 | Lighthouse reports `ERRORED_DOCUMENT_REQUEST` (404); replace with existing public routes, keep assertions unchanged.                     | `8aa80b2eb6a52f095e18d7f8fe4f245728baf0724cfba0675a5faf7e23063514` |

Statuses: open · confirmed · fixed · rejected · wontfix.

## Evidence ledger

One row per observado conclusion (`references/evidence.md`). `razonado` items go under
"Hypotheses" below, never here.

`Admit` holds ONE bare shell command, with no backticks and no placeholders, because `Executed` is
prose a human reads and `Admit` is the command the binary runs. `Digest` holds the `sha256:` digest of the
canonical output, written by `rdd-plus plan admit --execute --record <id>` rather than by hand.

A pin only means something over output that holds still, so `--execute` runs each admitted command twice:
the second run is the probe, and a row whose two observations disagree is refused as unstable instead of
pinned. `Normalize` is the escape hatch for the part of an output that legitimately moves, such as an
elapsed time: it holds a Go regular expression whose every match becomes `X` before hashing. Leave it empty
the first time and fill it only when the probe names what moves, keeping it as narrow as that part — a
pattern broad enough to swallow the output turns the pin into decoration. `plan check` requires none of
these columns; `plan admit` refuses a row whose `Admit` is absent, whose `Digest` is unpinned, or whose
output does not hold still.

`Mode` says where the observation was taken, and a pin is only comparable inside the mode it was taken in,
because the same command digests differently in a container than on this machine. An empty cell means `host`,
which is where every pin taken before the column existed was taken. `--sandbox` runs each admitted command in
a container with the tree mounted read-only and no network, and a row that tries to write is refused instead
of admitted. `--record` writes both cells, so recording is how a row's mode gets set; a row pinned in one mode
and checked in the other is refused as a mode mismatch, rather than as a digest mismatch that would say
nothing about why the digests disagree. Only a pin has a mode: a row that carries no digest is refused for the
missing pin, not told it was pinned somewhere.

`Mutate` is the machine half of a falsifiability claim: `<old> => <new> @ <path>:<line>`, one textual edit whose
old text must occur exactly once in that file, naming a file inside the tree. It is a value and not a command
because a replay has to be able to undo exactly what it did, and an edit admits an exact inverse while a
command does not. A row that declares one is claiming its own command goes red under the edit and green
without it, so the claim is checked rather than believed: `plan admit` refuses a mutation it cannot parse, find,
or tell apart from another, and under `--sandbox` it replays the claim — the edit lands on a copy of the tree git
knows, the command must fail there, the file is put back and its bytes verified, and the command must pass again.
A row whose command survives the edit, or whose restored half fails, is refused; outside `--sandbox` there is no
copy to edit and put back, so the claim is refused rather than admitted unchecked.

| Id                  | Claim                                                                                                                                        | Executed                                           | Admit                                                                                                                                                                                                                                                                          | Inputs and parameters                                                                                         | Observed                                                                                 | Digest | Normalize | Mode           | Mutate | Mutation or negative control → result                                                                                  | Reproduction                                                | Label (`observado` / `razonado`, literal) |
| ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ------ | --------- | -------------- | ------ | ---------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- | ----------------------------------------- |
| E-CI-AUTH           | If the auth-required lane points at the placeholder host, login fails before bootstrap                                                       | GitHub Actions job 108107458151 in run 36146073359 | `rtk proxy gh run view 36146073359 --json headSha,jobs`                                                                                                                                                                                                                        | PR head `cad6bc81`; env `NEXT_PUBLIC_SUPABASE_URL=https://example.supabase.co`; local credentials unavailable | `Login failed before bootstrap: Failed to fetch`; 22 tests did not run                   | -      |           | github-actions | none   | Baseline main run 35376754583 fails the same auth lane; PR changed only P2P/tests/ignore.                              | Run the auth-required Playwright lane with placeholder URL  | observado                                 |
| E-CI-RLS            | If DB/RLS Jest tests use the placeholder host, network setup fails before RLS assertions                                                     | GitHub Actions job 108107458436 in run 36146073359 | `rtk proxy gh run view 36146073359 --json headSha,jobs`                                                                                                                                                                                                                        | PR head `cad6bc81`; test `tests/db/rate-history-scrape-attempts-rls.test.ts`                                  | `getaddrinfo ENOTFOUND example.supabase.co`; 8 suites / 22 tests failed                  | -      |           | github-actions | none   | Baseline main run 35376754583 also has Required checks failure; eval-gate local Supabase job passes.                   | Run `npm test -- --runInBand` with workflow env             | observado                                 |
| E-CI-K6             | If k6 is pointed at the placeholder host, the smoke error-rate/p99 thresholds go red                                                         | GitHub Actions job 108107463757 in run 36146073354 | `rtk proxy gh run view 36146073354 --json headSha,jobs`                                                                                                                                                                                                                        | 5 VUs, 30s; placeholder Supabase host; PERF credentials unset                                                 | `example.supabase.co` DNS failure; `http_req_failed=67.10%`; `p(99)=3.12s`; exit 99      | -      |           | github-actions | none   | Main nightly performance run 36113658364 also fails k6/Lighthouse classes.                                             | Run existing k6 smoke scenario with current PR workflow env | observado                                 |
| E-CI-LH             | If Lighthouse targets `/dashboard`, the run reports document request 404                                                                     | GitHub Actions job 108107463872 in run 36146073354 | `rtk proxy gh run view 36146073354 --json headSha,jobs`                                                                                                                                                                                                                        | Lighthouse CI, target `http://localhost:3000/dashboard`                                                       | `ERRORED_DOCUMENT_REQUEST`, status 404                                                   | -      |           | github-actions | none   | Baseline main nightly run 36113658364 also has Lighthouse failure.                                                     | Run LHCI against the configured route list                  | observado                                 |
| E-CI-MAIN           | If the base `main` workflow has the same failures, they predate PR #88                                                                       | GitHub run 35376754583 on main                     | `rtk proxy gh run view 35376754583 --json headSha,jobs`                                                                                                                                                                                                                        | Main SHA `8458b23e0418986b1d71eaf9b897e78034d3cc4a`                                                           | E2E no-auth, E2E auth-required, and Required checks all failed; Eval harness gate passed | -      |           | github-actions | none   | Negative control: same failure class before the CI PR and before the P2P changes.                                      | Compare baseline main jobs with PR #88 checks               | observado                                 |
| E-CI-LOCAL-DOMNODE  | If unit/node tests inherit the live DB env, they stop using their mocked Supabase boundary; empty step env must select only `dom` and `node` | Independent verifier task muh5ln66-e-h8vn          | NEXT_PUBLIC_SUPABASE_URL= NEXT_PUBLIC_SUPABASE_ANON_KEY= SUPABASE_SERVICE_ROLE_KEY= rtk proxy npm run test:ci -- --selectProjects dom node                                                                                                                                     | Empty Supabase env; projects `dom` and `node`                                                                 | `293 suites passed, 3 skipped; 2,092 tests passed, 12 skipped`                           | -      |           | host           | none   | The separate db project remains explicitly invoked by `npm run test:db`; this local run does not claim DB integration. | Run the split `dom/node` Jest command with blank env        | observado                                 |
| E-CI-LOCAL-HELPER   | If local env mapping accepts a remote host or newline injection, the safety contract goes red                                                | Independent verifier task muh5ln66-e-h8vn          | rtk proxy npx jest tests/node/scripts/prepare-local-supabase-ci.test.ts --runInBand                                                                                                                                                                                            | Loopback URL plus remote/non-loopback/newline negative cases                                                  | `1 suite, 5 tests passed`                                                                | -      |           | host           | none   | Remote and newline inputs are explicit negative controls; mapping test confirms only loopback inputs are exported.     | Run the local Supabase env helper unit suite                | observado                                 |
| E-CI-LOCAL-ENV      | If the CI helper entrypoint is invoked with its local test defaults, it writes env without remote network or errors                          | Parent runner                                      | env -u NEXT_PUBLIC_SUPABASE_URL -u NEXT_PUBLIC_SUPABASE_ANON_KEY -u SUPABASE_SERVICE_ROLE_KEY GITHUB_ENV=/dev/null rtk proxy npx tsx scripts/testing/prepare-local-supabase-ci.ts                                                                                              | Env unset; GITHUB_ENV points to /dev/null; no provision flag                                                  | Exit 0; no stdout/stderr; no values printed                                              | -      |           | host           | none   | The helper is exercised without starting Docker/Supabase and suppresses all credential values.                         | Run the environment export CLI with GITHUB_ENV=/dev/null    | observado                                 |
| E-CI-LOCAL-OXLINT   | If the new helper/tests violate lint rules, scoped oxlint exits non-zero                                                                     | Independent verifier task muh5ln66-e-h8vn          | rtk proxy npx oxlint --quiet scripts/testing/prepare-local-supabase-ci.ts tests/node/scripts/prepare-local-supabase-ci.test.ts                                                                                                                                                 | CI env helper and helper tests                                                                                | No diagnostics; exit 0                                                                   | -      |           | host           | none   | Scoped static check observes only the implementation surfaces.                                                         | Run scoped oxlint command                                   | observado                                 |
| E-CI-LOCAL-TSC      | If the helper or workflow-related TypeScript is ill-typed, project type-check exits non-zero                                                 | Independent verifier task muh5ln66-e-h8vn          | rtk proxy npm run type-check                                                                                                                                                                                                                                                   | Repository type-check config                                                                                  | `tsc --noEmit -p tsconfig.typecheck.json`; no errors                                     | -      |           | host           | none   | Type-check passed on the isolated CI worktree.                                                                         | Run npm run type-check                                      | observado                                 |
| E-CI-LOCAL-PRETTIER | If any changed workflow/helper/config file is not formatted, Prettier check exits non-zero                                                   | Independent verifier task muh5ln66-e-h8vn          | rtk proxy npx prettier --check .github/workflows/ci.yml .github/workflows/perf-pr.yml .github/actions/local-supabase/action.yml scripts/testing/prepare-local-supabase-ci.ts tests/node/scripts/prepare-local-supabase-ci.test.ts tests/performance/lighthouse/lighthouserc.js | Six implementation surfaces                                                                                   | All files formatted; exit 0                                                              | -      |           | host           | none   | Formatting check covers all changed implementation files.                                                              | Run the exact Prettier check                                | observado                                 |
| E-CI-LOCAL-DIFF     | If the final diff contains whitespace errors, git diff --check exits non-zero                                                                | Independent verifier task muh5ln66-e-h8vn          | rtk proxy git diff --check                                                                                                                                                                                                                                                     | Current CI worktree diff                                                                                      | No output; exit 0                                                                        | -      |           | host           | none   | No whitespace errors observed.                                                                                         | Run git diff --check                                        | observado                                 |

### Hypotheses (razonado)

| Hypothesis                                                                                                            | Probe that would settle it                                                                                                                       |
| --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| The local Supabase baseline at port 54421 provides every API/Auth behavior required by the DB, auth, and k6 journeys. | Run the same CI workflow on the CI repair branch with a fresh local Supabase stack; inspect the DB/RLS, auth bootstrap, and k6 step outputs.     |
| The public Lighthouse pages `/`, `/privacy`, `/terms`, and `/waitlist` satisfy the existing strict assertions.        | Run LHCI in the CI repair branch and inspect each URL's status and assertion report; change routes only if a reachable public route still fails. |

## Calibration history

One row per calibration run (`references/calibration.md`); never overwritten.

| Date | Skill version | K   | Found | Recall | Misses (file:line operator, why) | False positives |
| ---- | ------------- | --- | ----- | ------ | -------------------------------- | --------------- |

## Blocked by testability

| Target                                  | Rung  | Why                                                                                                                        | Minimal change that opens it                                                                                                                                |
| --------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CI-dependent real Supabase verification | L4/L5 | Local Docker/Supabase execution was intentionally not authorized; DB/Auth/k6/Lighthouse integration has not been observed. | Open the isolated CI PR and use its ephemeral GitHub Actions runners to start the local baseline, capture runtime output, and stop the stack in `always()`. |

## Remaining, in order

1. Run the CI PR's local Supabase DB/RLS, auth-required, and no-auth jobs — L4/L5 — `database-persistence-testing`, `real-run-validation`.
2. Observe k6 smoke and public-route Lighthouse on the ephemeral runner without changing thresholds — L3/L4 — `runtime-reliability-testing`, `real-run-validation`.
3. Record results, settle findings, run native review, and open the separate CI PR — L5 — `test-strategy`.
