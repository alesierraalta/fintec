import { MainLayout } from '@/components/layout/main-layout';
import { TransferContent } from '@/components/transfers/transfer-content';

export default function TransfersPage() {
  return (
    <MainLayout>
      <TransferContent />
    </MainLayout>
  );
}
