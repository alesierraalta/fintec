# Apply Progress: transfer-same-currency-no-rate

## Status consumed
- schemaName: spec-driven
- changeName: transfer-same-currency-no-rate
- artifactStore: openspec
- planningHome.root: /home/alesierraalta/documents/projects/fintec-worktrees/transfer-same-currency-no-rate
- changeRoot: openspec/changes/transfer-same-currency-no-rate
- artifacts: proposal=done, spec=missing at requested `specs/transfers/spec.md`, design=done, tasks=done, applyProgress=partial
- actionContext: mode=repo-local; workspaceRoot=/home/alesierraalta/documents/projects/fintec-worktrees/transfer-same-currency-no-rate; allowedEditRoots=[/home/alesierraalta/documents/projects/fintec-worktrees/transfer-same-currency-no-rate]
- strict_tdd: active (openspec/config.yaml; runner=jest)
- workload gate: Decision needed before apply: Yes; Chained PRs recommended: Yes; Chain strategy: exception-ok (parent approved); 400-line budget risk: High
- CodeGraph/context-mode: MCP unavailable in this runtime; no structural collection was performed through those services.

## Result
Blocked before implementation. The workload gate requires an explicit delivery decision before code changes: choose `auto-chain` (or an explicit chained/stacked PR mode and assigned work-unit slice), or provide `size:exception` / `exception-ok` approval accepting the single-PR change above the 400-line budget. `Chain strategy: exception-ok (parent approved)` is not a resolved delivery path.

No production or test code was changed. No task checkboxes were updated.

## Verification
- Commands run: status/artifact inspection only.
- Tests run: none.

## Remaining implementation tasks
- [ ] 1.1 Create `tests/lib/transfer-policy.test.ts` with failing unit cases for `isSameCurrencyTransfer`, effective rate `1`, identity destination minor units, obsolete-rate ignoring, safe `totalDebitMinor`, and unchanged cross-currency conversion; cover equal currencies, missing currencies, zero/negative/overflow inputs, and USD/VES regression behavior. <!-- sdd-owner: implementation -->
- [ ] 1.2 Create `tests/lib/validations/transfers.test.ts` with failing cases for exact major-to-minor parsing and optional commission normalization: blank/null absent, explicit zero preserved, valid integer minor units accepted, and negative, non-finite, fractional, over-precision, unsafe, and overflowing values rejected. <!-- sdd-owner: implementation -->
- [ ] 1.3 Extend `tests/node/api/transfers-route.test.ts` and `tests/node/api/transfers-envelope.test.ts` with failing POST cases proving `commissionMinor` is normalized and propagated, invalid commission is a 400 validation error, an obsolete same-currency rate does not block the request, and the response exposes commission/total debit. <!-- sdd-owner: implementation -->
- [ ] 1.4 Add `tests/node/repositories/transfers-commission.test.ts` and extend `tests/node/repositories/transfer-projections.test.ts` with failing adapter cases for `p_commission_minor`, nullable aggregate reads, `commissionMinor`/`totalDebitMinor` mapping, and historical transfers without a fee. <!-- sdd-owner: implementation -->
- [ ] 1.5 Add `tests/node/api/rpc-create-transfer-atomicity.test.ts` as a disposable/live-gated Supabase integration suite with failing cases for commission debit, destination credit of amount only, nullable fee, shared transfer IDs, insufficient total balance, and rollback with no partial rows/balance changes; update `tests/node/repositories/financial-rpc-authorization-migration.test.ts` expectations for the canonical RPC signature. <!-- sdd-owner: implementation -->
- [ ] 1.6 Extend `tests/components/desktop-transfer-exchange-sync.test.tsx` and `tests/components/mobile-transfer-exchange-sync.test.tsx` with failing same-currency scenarios: both rate hooks receive `enabled: false`, provider fetch functions are not called, rate controls are hidden, target preview is identity/display-only, `Comisión (opcional)` is visible with source currency/helper text, and the summary shows commission plus total source debit. <!-- sdd-owner: implementation -->
- [ ] 2.1 Create `lib/transfers/transfer-policy.ts` with pure `isSameCurrencyTransfer`, effective-rate, identity/cross-currency target-minor, and overflow-safe total-debit helpers; make `isSameCurrencyTransfer` require two equal currencies and make same-currency policy ignore client rates. Done when `tests/lib/transfer-policy.test.ts` passes without provider/UI dependencies. <!-- sdd-owner: implementation -->
- [ ] 2.2 Modify `lib/transfers/exchange-calculations.ts` so `isExchangeableTransferPair` excludes equal currencies and delegates classification to the shared policy while preserving existing USD/VES rate scaling, rounding, and reverse-edit behavior. Done when policy tests and existing `tests/lib/transfer-exchange-calculations.test.ts` pass. <!-- sdd-owner: implementation -->
- [ ] 2.3 Create `lib/validations/transfers.ts` with the exact source major-to-minor parser, optional commission parser, integer/safe-range guards, and safe `totalDebitMinor` helper; do not silently round invalid precision or overflow. Done when `tests/lib/validations/transfers.test.ts` passes. <!-- sdd-owner: implementation -->
- [ ] 2.4 Modify `types/domain.ts` and `repositories/contracts/transfers-repository.ts` to use optional `commissionMinor` in `CreateTransferDTO`, `Transfer`, `TransferRecord`, and create-result contracts, expose `totalDebitMinor`, and document that commission is source-currency minor units while persistence remains `fee_minor`. Done when all repository consumers typecheck and the policy/validation suites pass. <!-- sdd-owner: implementation -->
- [ ] 3.1 Create the next ordered migration `supabase/migrations/<next-timestamp>_add_transfer_commission_and_identity.sql` as the sole forward schema/RPC change: add nullable non-negative `transfers.fee_minor` idempotently, drop known conflicting `create_transfer` signatures, create the canonical signature with `p_commission_minor BIGINT DEFAULT NULL`, preserve ownership/grants/schema reload, and never backfill historical fees. Verify the migration parses and is safe to re-run in a disposable database. <!-- sdd-owner: implementation -->
- [ ] 3.2 Update `supabase/schemas/baseline.sql` to match the migration exactly: nullable `fee_minor`, canonical `create_transfer` body/signature/owner, same-currency rate `1` and identity target, total-debit lock/check, aggregate insert, shared `transfer_id` linkage, and authenticated/service-role grants. Done when the static migration/baseline assertions in `tests/node/repositories/financial-rpc-authorization-migration.test.ts` pass. <!-- sdd-owner: implementation -->
- [ ] 3.3 Modify `repositories/supabase/types.ts`, `repositories/supabase/mappers.ts`, and `repositories/supabase/transfer-projections.ts` to represent `fee_minor` as nullable, map it to optional `commissionMinor`, add the aggregate projection, and keep transaction projections ownership-safe and money-complete. Done when mapper/projection tests cover fee present, zero, and historical null. <!-- sdd-owner: implementation -->
- [ ] 3.4 Modify `repositories/supabase/transfers-repository-impl.ts` to revalidate amount/commission, resolve account currencies, apply the shared same-currency policy, send `p_commission_minor` as null or an integer, query the aggregate by transaction `transfer_id`, and return `commissionMinor` plus safe `totalDebitMinor`; preserve actionable stale-schema errors. Done when `tests/node/repositories/transfers-commission.test.ts`, `transfer-projections.test.ts`, and `transfers-create-error.test.ts` pass. <!-- sdd-owner: implementation -->
- [ ] 3.5 Modify `app/api/transfers/route.ts` to accept blank/null/zero commission correctly, reject invalid minor-unit values before repository invocation, stop rejecting an obsolete same-currency client rate, pass `commissionMinor ?? undefined`, and include commission/total debit in GET/POST envelopes without changing authentication or same-account checks. Done when both route suites pass and the repository mock receives the exact normalized input. <!-- sdd-owner: implementation -->
- [ ] 3.6 Preserve local parity in `repositories/local/db.ts` and `repositories/local/transactions-repository-impl.ts`: carry optional commission on the transfer aggregate and execute paired transaction rows, balance updates using total debit, and aggregate write in one Dexie transaction, without adding an unnecessary index or historical backfill. Cover the local path with focused repository tests or extend the existing local transaction test target. <!-- sdd-owner: implementation -->
- [ ] 3.7 Update any remaining RPC caller in `repositories/supabase/transactions-repository-impl.ts` and its focused tests so supported transfer creation sends the canonical `p_commission_minor`, uses identity amounts for equal currencies, and does not reintroduce an overloaded/legacy function contract. <!-- sdd-owner: implementation -->
- [ ] 4.1 Modify `components/transfers/desktop-transfer.tsx` to derive `ratesEnabled = Boolean(from && to && !isSameCurrencyTransfer(...))`, pass it to `useBCVRates`/`useBinanceRates`, clear obsolete rate state, use policy target minor identity for equal currencies, and retain current cross-currency exchange synchronization. <!-- sdd-owner: implementation -->
- [ ] 4.2 Modify `components/transfers/mobile-transfer.tsx` with the same policy-driven `ratesEnabled`, state reset, identity preview, and cross-currency preservation; do not duplicate business rules beyond presentation/layout differences. <!-- sdd-owner: implementation -->
- [ ] 4.3 In `components/transfers/desktop-transfer.tsx` and `components/transfers/mobile-transfer.tsx`, add an accessible `Comisión (opcional)` section after both accounts are selected, show the source currency suffix and helper text explaining the additional debit, preserve blank versus zero, convert exactly to `commissionMinor`, validate total source debit locally, send the field in POST, and show commission plus total source debit in the confirmation preview. <!-- sdd-owner: implementation -->
- [ ] 4.4 Complete the desktop/mobile GREEN work against `tests/components/desktop-transfer-exchange-sync.test.tsx` and `tests/components/mobile-transfer-exchange-sync.test.tsx`; verify same-currency rate controls are absent, no BCV/Binance request starts, target amount equals source minor units, and cross-currency manual/auto editing remains unchanged. <!-- sdd-owner: implementation -->
- [ ] 5.1 Run the focused RED/GREEN suites for policy, validation, route, repository projections, desktop exchange sync, and mobile exchange sync; resolve only contract mismatches demonstrated by those tests. <!-- sdd-owner: implementation -->
- [ ] 5.2 Run `tests/node/api/rpc-create-transfer-atomicity.test.ts` against disposable Supabase and verify both success and failure invariants: source balance subtracts `amountMinor + commissionMinor`, destination adds only `amountMinor`, one aggregate row is written, both legs share its ID, `fee_minor` is nullable, and any error leaves no transfer/aggregate/balance mutation. <!-- sdd-owner: implementation -->
- [ ] 5.3 Exercise historical read compatibility through `tests/node/repositories/transfer-projections.test.ts` and the transfers API: `fee_minor IS NULL` must omit/return absent `commissionMinor`, zero must remain zero, and aggregate data must not leak transfers outside the owned-account scope. <!-- sdd-owner: implementation -->
- [ ] 5.4 Run cross-currency regression coverage for `tests/lib/transfer-exchange-calculations.test.ts`, both exchange-sync suites, and existing transfer response tests; confirm valid rate validation, rounding, target recalculation, and provider-backed behavior remain intact for USD/VES and VES/USD. <!-- sdd-owner: implementation -->
- [ ] 5.5 Run repository verification commands from `openspec/config.yaml`: focused Jest, full Jest, typecheck, lint, formatter check, and the available integration/E2E gates; record any live-Supabase gate requirements rather than weakening tests. <!-- sdd-owner: implementation -->
- [ ] 6.1 Refactor `components/transfers/desktop-transfer.tsx`, `components/transfers/mobile-transfer.tsx`, and `lib/transfers/exchange-calculations.ts` to remove duplicated same-currency conditionals, retain one policy seam, and keep UI text/field semantics consistent; rerun both component suites. <!-- sdd-owner: implementation -->
- [ ] 6.2 Review `types/domain.ts`, Supabase mappers/projections/types, `supabase` migration/baseline, and API response names for a single domain `commissionMinor` vocabulary with `fee_minor` only at persistence boundaries; rerun typecheck and migration-contract tests. <!-- sdd-owner: implementation -->
- [ ] 6.3 Confirm the final diff contains no historical fee backfill, no second fee column, no provider fetch for equal currencies, no client-only balance enforcement, and no destructive rollback; run the full focused verification set before handing off to the parent. <!-- sdd-owner: implementation -->

## Parent-owned deferred lifecycle actions
- Start/reuse bounded review per chained PR slice.
- Decide chain strategy and perform lifecycle gate after implementation verification and review.
