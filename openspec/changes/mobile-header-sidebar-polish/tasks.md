# Tasks: Mobile Header and Sidebar Polish

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 650–900 lines across approximately 14–18 source, test, and fixture files |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 → PR 2 → PR 3 |
| Delivery strategy | ask-on-risk |
| Chain strategy | exception-ok (single PR, parent approved size:exception) |

Decision needed before apply: No - parent approved single-PR exception
Chained PRs recommended: Yes
Chain strategy: exception-ok (single PR, parent approved size:exception)
400-line budget risk: High

## Scope, dependencies, and work-unit boundaries

- Follow strict TDD: complete RED tests before the corresponding GREEN implementation, then TRIANGULATE cross-surface behavior, then REFACTOR.
- Preserve `SidebarProvider`, desktop `Sidebar`/`Header` behavior, route semantics, `!isNative` download behavior, portal roots, `useMobileChromeGeometry`, and existing `+Nueva` bottom-right FAB behavior.
- PR 1 boundary: typed route metadata and BottomNav. It starts with route-role RED tests and finishes with five primary links, wrapping labels, and focused Jest verification.
- PR 2 boundary: dedicated mobile drawer, header ownership, main-layout wiring, and removal of the logo FAB. It starts after PR 1 metadata is available and finishes with drawer lifecycle/focus/native-back verification.
- PR 3 boundary: account/status polish, responsive/accessibility E2E coverage, cleanup, and full verification. Roll back any unit by reverting its source and focused tests; no data migration or data rollback is required.

## Phase 1 — RED: behavioral contracts first

- [x] 1.1 Create `tests/components/navigation.test.ts` with failing assertions for the typed route-role contract: exactly five primaries in order (`/`, `/accounts`, `/transactions`, `/transfers`, `/goals`), `Transacciones` for `/transactions`, and the complementary secondary inventory (`/recurring`, `/categories`, `/budgets`, `/reports`, `/calculator`, `/debts`, `/p2p-offers`, `/backups`, `/chat`, `/settings`) with conditional Admin; assert `/goals` and `/pricing` are not duplicated in the secondary navigation. Done when the test names the expected href/label arrays and fails against the current duplicated literals. <!-- sdd-owner: implementation -->
- [x] 1.2 Extend `tests/components/mobile-nav.test.tsx` with failing cases for viewports 320/360/390/430 (using the component contract or responsive test helpers), readable `Transacciones`, five links in the required order, no `truncate`/clipping classes, wrapping/fit-safe label classes, minimum 44px interactive targets, active `/transactions` state, and safe-area padding. Done when the assertions cover all five widths and fail against the current `Gastos`, `overflow-hidden`, and `truncate` implementation. <!-- sdd-owner: implementation -->
- [x] 1.3 Create `tests/components/mobile-drawer.test.tsx` with failing cases for a portaled fixed left `aside`/dialog that is not a BottomSheet and does not use `w-64`; cover overlay dismissal, labelled 44px close control, `aria-modal`, `aria-current`, route selection closing, Escape closing, focus restoration to the header opener, safe-area top/left/bottom classes, and the complete premium/Admin-filtered secondary inventory. Done when the test drives `MobileDrawer` through `open`/`onClose` and fails because the component does not yet exist. <!-- sdd-owner: implementation -->
- [x] 1.4 Add or extend `tests/components/header.test.tsx` with failing mobile contracts for grouped controls (menu then Downloads on the left; notifications and user on the right), an absolute bounded centered logo, no horizontal overflow at 320–430px, unchanged `/download` and `aria-label="Descargar APK Android Beta"` behavior for non-native state, and hidden download behavior for native state. Done when both native branches and `aria-expanded`/`aria-controls` ownership of the single menu trigger are asserted. <!-- sdd-owner: implementation -->
- [x] 1.5 Add failing account/layout assertions in `tests/components/header.test.tsx`, `tests/app/app-shell-scroll-contract.test.tsx`, and a focused main-layout test if needed: mobile user surface contains ThemeToggle and compact readable `Premium Activo`; mobile shell mounts one drawer path, no `MobileMenuFAB`, no mobile `Sidebar` wrapper/backdrop, and exactly one bottom-right `+Nueva` action while desktop still mounts the existing `Sidebar`. Done when current mocks/imports expose the expected failure and the desktop invariant is explicitly covered. <!-- sdd-owner: implementation -->
- [x] 1.6 Update `tests/integration/components/transient-back-adapters.test.tsx` with failing registration assertions for `mobile-drawer` at priority 95 only while open, and add a dismissal/focus regression for the header-owned opener; remove the old `mobile-menu` expectation. Done when the integration test distinguishes the drawer back adapter from modal, alert, and transaction-detail adapters. <!-- sdd-owner: implementation -->
- [x] 1.7 Replace the old assertions in `tests/e2e/mobile-navigation.spec.ts` and `tests/e2e/mobile-menu-fab-regression.spec.ts` with failing Playwright contracts for the header trigger and side drawer: no URL change on open, route dismissal, Escape/overlay close, 320/360/390/430 projects or viewport matrix, no horizontal overflow, safe-area geometry, all five primary links, exactly one bottom-right `+Nueva`, absence of the bottom-left logo trigger, and native/non-native download visibility. Done when the E2E suite targets the new shell route rather than `title="Más opciones"`. <!-- sdd-owner: implementation -->

## Phase 2 — GREEN: implement the smallest behavior that satisfies RED

- [x] 2.1 Create `components/layout/navigation.ts` with the `NavigationItem` type and shared `mobilePrimaryNavigation`/`mobileSecondaryNavigation` metadata, including `mobileLabel`, optional `desktopLabel`, icon, premium flag, and conditional Admin composition. Done when `tests/components/navigation.test.ts` passes and `Sidebar`/`MobileNav` can consume one route source without changing desktop labels or adding `/pricing` as a navigation item. <!-- sdd-owner: implementation -->
- [x] 2.2 Create `components/layout/mobile-drawer.tsx` with `MobileDrawerProps = { open: boolean; onClose: () => void }`, portal rendering to the existing host, fixed left `aside`/dialog geometry `min(20rem, calc(100vw - 1rem))`, overlay, scrollable secondary links, safe-area insets, 44px controls, `aria` labelling, route-close behavior, Escape handling, focus restoration, and one `useNativeBackNavigation` registration with id `mobile-drawer` and priority 95. Done when all drawer unit/integration RED tests pass and the implementation contains no BottomSheet layout or `w-64`. <!-- sdd-owner: implementation -->
- [x] 2.3 Modify `components/layout/header.tsx` so the mobile header has shrink-safe left/right groups, moves the existing non-native Downloads link beside the menu on the left, keeps NotificationBell and user controls on the right, and bounds the centered `FinTecLogo` without collision at 320–430px; keep the desktop branch and exact native download condition/route/label unchanged. Done when header unit tests pass for both native states and all controls remain at least 44px. <!-- sdd-owner: implementation -->
- [x] 2.4 Modify `components/layout/main-layout.tsx` to render `MobileDrawer` from `SidebarProvider` state, remove the mobile `Sidebar`/backdrop `w-64` path and the `MobileMenuFAB` import/render, retain the desktop Sidebar wrapper, `MobileNav`, `+Nueva` allowlist/position, transaction modal, and existing chrome hiding rules. Done when the shell has the header as the sole secondary-navigation trigger and the app-shell scroll contract remains green. <!-- sdd-owner: implementation -->
- [x] 2.5 Modify `components/layout/sidebar.tsx` to consume `components/layout/navigation.ts` for desktop route generation while preserving desktop presentation, admin gating, active states, and upgrade behavior; ensure mobile no longer depends on Sidebar spacing or its large status/preferences section. Done when `tests/components/admin-sidebar.test.tsx` and desktop premium regressions continue to pass without a mobile `w-64` dependency. <!-- sdd-owner: implementation -->
- [x] 2.6 Modify `components/layout/mobile-nav.tsx` to consume the five-item metadata, render `Transacciones`, remove truncation/clipping, and use fit-safe flex sizing, `min-w-[44px]`, `min-h-[52px]`, `whitespace-normal`, and `break-words` with safe-area-aware chrome geometry. Done when the mobile-nav RED tests pass for order, active state, wrapping, target size, and all safe-area styles. <!-- sdd-owner: implementation -->
- [x] 2.7 Modify `components/subscription/premium-status-card.tsx` with a readable compact/badge variant while preserving the desktop variant, and use that compact variant from the mobile account surface in `components/layout/header.tsx` next to ThemeToggle; do not allocate a large Premium section in the mobile drawer. Done when premium, free, loading, and owner/admin states show the correct badge/upgrade behavior and theme is available from the user surface rather than Sidebar mobile chrome. <!-- sdd-owner: implementation -->
- [x] 2.8 Delete `components/layout/mobile-menu-fab.tsx` and remove or replace its obsolete fixtures in `components/testing/mobile-menu-fab-regression-harness.tsx`, `app/dev/mobile-menu-fab-regression/page.tsx`, and `tests/components/mobile-menu-fab.test.tsx` with a header/drawer regression harness under `components/testing/` and `app/dev/` as needed by the new E2E route. Done when repository imports contain no `MobileMenuFAB`, no bottom-left logo trigger, and the replacement harness exposes the real header-owned drawer. <!-- sdd-owner: implementation -->

## Phase 3 — TRIANGULATE: verify behavior across surfaces and environments

- [x] 3.1 Run focused Jest for `tests/components/navigation.test.ts`, `tests/components/mobile-nav.test.tsx`, `tests/components/mobile-drawer.test.tsx`, `tests/components/header.test.tsx`, `tests/components/admin-sidebar.test.tsx`, `tests/providers/sidebar-premium-indicator.test.tsx`, and `tests/app/app-shell-scroll-contract.test.tsx`; reconcile any mismatch between shared metadata, premium/Admin filtering, desktop invariants, and rendered accessibility names before proceeding. Done when the focused component suite is green without weakening the specified assertions. <!-- sdd-owner: implementation -->
- [x] 3.2 Run `tests/integration/components/transient-back-adapters.test.tsx` and verify open/close lifecycle, Escape, overlay, route-selection close, focus return, and native-back priority ordering with the real `SidebarProvider`/portal path. Done when only the open dedicated drawer registers `mobile-drawer` and the opener regains focus on dismissal. <!-- sdd-owner: implementation -->
- [x] 3.3 Run the configured Playwright mobile projects against the replacement dev harness and `tests/e2e/mobile-navigation.spec.ts` at 320, 360, 390, and 430px; inspect bounding boxes for header controls, drawer width/side placement, bottom nav labels, safe-area offsets, no horizontal overflow, 44px targets, and exactly one bottom-right `+Nueva`. Done when both Mobile Chrome and Mobile Safari scenarios pass, including route dismissal and absence of the old logo FAB. <!-- sdd-owner: implementation -->
- [x] 3.4 Exercise native and non-native runtime states in the header E2E/component coverage and verify `/download` remains discoverable only under the existing non-native rule, while desktop smoke coverage confirms unchanged Sidebar/Header roles and layout at the desktop breakpoint. Done when download semantics and desktop navigation are behaviorally identical to the pre-change contract. <!-- sdd-owner: implementation -->

## Phase 4 — REFACTOR: consolidate and close the change

- [x] 4.1 Remove stale mocks, imports, route references, screenshots, and test descriptions from `tests/integration/components/transient-back-adapters.test.tsx`, `tests/app/app-shell-scroll-contract.test.tsx`, `tests/e2e/mobile-menu-fab-regression.spec.ts`, `components/testing/mobile-menu-fab-regression-harness.tsx`, and `app/dev/mobile-menu-fab-regression/page.tsx`; update `tests/e2e/mobile-navigation.spec.ts` and any replacement harness route consistently. Done when `MobileMenuFAB`, `mobile-menu-fab`, `mobile-menu`, and the old BottomSheet expectations have no active implementation/test references. <!-- sdd-owner: implementation -->
- [x] 4.2 Refactor `components/layout/navigation.ts`, `components/layout/sidebar.tsx`, `components/layout/mobile-nav.tsx`, and `components/layout/mobile-drawer.tsx` to keep route metadata single-sourced, preserve readable mobile labels versus desktop labels, and centralize repeated filtering/active-link/accessibility logic without changing behavior. Done when focused Jest and E2E assertions remain green and no primary/secondary route is duplicated. <!-- sdd-owner: implementation -->
- [x] 4.3 Run final formatting and repository verification with `npx prettier --check` on changed files, `npm run type-check`, `npm run lint`, focused `npm test -- --runInBand` suites, configured mobile E2E, and applicable performance/mutation checks from `openspec/config.yaml`; record any environment-only skip explicitly for the parent. Done when type-check, lint, tests, and configured browser checks pass or have documented, reproducible external blockers. <!-- sdd-owner: implementation -->

## Parent-owned review and lifecycle gates

- [ ] Review the completed implementation in bounded chunks according to the selected chain strategy and confirm the 400-line threshold/PR split before apply starts. <!-- sdd-owner: parent -->
- [ ] Confirm the final diff contains no implementation of `MobileMenuFAB`, no mobile reuse of desktop `Sidebar` `w-64`, and no regression to native download or desktop behavior before closing the SDD change. <!-- sdd-owner: parent -->